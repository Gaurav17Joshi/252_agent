# Core
anthropic>=0.40.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0

# Optional: real SAM2 (uncomment when checkpoint is available)
# segment-anything-2

# Optional: real SAM3D (install from source per repo instructions)
# sam3d

# Optional: mesh processing / validation
# trimesh>=4.0.0
# open3d>=0.17.0

# Optional: Blender Python API (only when running inside Blender)
# bpy  # provided by Blender's bundled Python; do not pip-install